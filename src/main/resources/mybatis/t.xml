<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.syzc.sseip.dao.TDao">
    <resultMap id="Group" type="com.syzc.sseip.entity.TTE">
        <id column="id" property="id"/>
    </resultMap>
    <!--
        <insert id="add" useGeneratedKeys="true" keyProperty="entity.id">
        </insert>
        <delete id="remove">
        </delete>
        <update id="update">
        </update>
        <select id="get" resultMap="Group">
        </select>
        <select id="list" resultMap="Group">
        </select>
        <select id="count" resultType="Long">
        </select>

        <select id="listAll" resultMap="Group">
        </select>
    -->
</mapper>